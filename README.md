# 🏛️ B36 Hall Management System
## نظام إدارة القاعات اللوجستية

نظام متكامل لإدارة القاعات وتتبع حركة الزوار باستخدام **Streamlit** و **Supabase**.

---

## 🚀 المميزات

### ✨ الوظائف الأساسية
- 📊 **لوحة معلومات تفاعلية** مع إحصائيات حية
- 🏛️ **إدارة القاعات** (إضافة، تعديل، حذف)
- ⏳ **إدارة قائمة الانتظار** الخارجية
- 📈 **تقارير وإحصائيات** مفصلة
- 🚨 **نظام البلاغات** للمشاكل الطارئة
- 📋 **سجل النشاطات** الكامل
- 👥 **إدارة المستخدمين** والصلاحيات

### 🎨 المميزات التقنية
- 🌐 **واجهة عربية** كاملة
- 📱 **تصميم متجاوب** يعمل على جميع الأجهزة
- 🔄 **تحديث تلقائي** للبيانات
- 📊 **رسوم بيانية تفاعلية** باستخدام Plotly
- 🔐 **نظام مصادقة** آمن
- 💾 **قاعدة بيانات سحابية** Supabase

---

## 📋 المتطلبات

### البرمجيات المطلوبة
- Python 3.8 أو أحدث
- pip (مدير الحزم)
- حساب Supabase (مجاني)

### المكتبات المطلوبة
```bash
streamlit==1.31.0
supabase==2.3.4
python-dotenv==1.0.0
pandas==2.1.4
plotly==5.18.0
pillow==10.2.0
```

---

## 🔧 التثبيت والإعداد

### الخطوة 1: إنشاء مشروع Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. قم بإنشاء حساب جديد (مجاني)
3. أنشئ مشروع جديد
4. احصل على:
   - **Project URL**: `https://xxxxxxxx.supabase.co`
   - **API Key (anon/public)**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

### الخطوة 2: إنشاء قاعدة البيانات

1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. افتح ملف `setup_database.sql`
3. انسخ المحتوى كاملاً
4. الصقه في SQL Editor
5. اضغط **Run** لتنفيذ الأوامر

> ✅ سيتم إنشاء جميع الجداول والبيانات التجريبية تلقائياً

### الخطوة 3: تثبيت المشروع

```bash
# 1. نسخ المشروع
git clone <repository-url>
cd b36-system

# 2. تثبيت المكتبات
pip install -r requirements.txt

# 3. إعداد ملف البيئة
# أنشئ ملف .env وأضف:
SUPABASE_URL=https://jeenoibxorvldajgyupb.supabase.co
SUPABASE_KEY=<your-api-key-here>
```

### الخطوة 4: تشغيل التطبيق

```bash
streamlit run main.py
```

سيفتح المتصفح تلقائياً على `http://localhost:8501`

---

## 🔐 بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: 1234
```

> ⚠️ **مهم**: غيّر كلمة المرور بعد أول تسجيل دخول!

---

## 📁 هيكل المشروع

```
b36-system/
├── main.py                 # التطبيق الرئيسي
├── database.py            # إدارة قاعدة البيانات
├── requirements.txt       # المكتبات المطلوبة
├── .env                   # بيانات الاتصال (لا تُرفع على Git)
├── setup_database.sql     # سكريبت إنشاء قاعدة البيانات
└── README.md             # هذا الملف
```

---

## 📊 استخدام النظام

### 1. لوحة المعلومات (Dashboard)
- عرض الإحصائيات الحية
- رسوم بيانية تفاعلية
- إدارة سريعة للقوائم
- آخر النشاطات

### 2. إدارة القاعات
- إضافة قاعة جديدة
- تسجيل دخول/خروج الزوار
- تعديل العدد يدوياً
- إيقاف/تفعيل القاعة

### 3. التقارير
- **إحصائيات عامة**: ملخص شامل
- **سجل النشاطات**: جميع العمليات
- **تقرير القاعات**: تفاصيل كل قاعة
- **البلاغات**: المشاكل والحلول

### 4. الإعدادات
- إدارة المستخدمين
- إعدادات النظام
- إعادة تعيين العدادات

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية

#### 1. **users** - المستخدمين
```sql
- id: معرّف فريد
- username: اسم المستخدم
- password: كلمة المرور
- full_name: الاسم الكامل
- role: الدور (ADMIN/STAFF/VIEWER)
```

#### 2. **halls** - القاعات
```sql
- id: معرّف فريد
- name: اسم القاعة
- type: النوع (MAIN/WAITING/INTERVIEW)
- capacity: السعة القصوى
- current: العدد الحالي
- status: الحالة (OPEN/PAUSED)
```

#### 3. **global_settings** - الإعدادات العامة
```sql
- outdoor_queue: قائمة الانتظار الخارجية
- served_count: عدد المخدومين
```

#### 4. **activity_logs** - سجل النشاطات
```sql
- user: المستخدم
- action: العملية
- details: التفاصيل
- timestamp: الوقت
```

#### 5. **incidents** - البلاغات
```sql
- description: وصف المشكلة
- priority: الأولوية
- status: الحالة
- hall_id: القاعة المتأثرة
```

---

## 🔌 API Reference

### database.py - الوظائف المتاحة

#### إدارة المستخدمين
```python
db = get_database()

# إنشاء مستخدم
db.create_user(username, password, full_name, role)

# المصادقة
user = db.authenticate_user(username, password)

# جلب جميع المستخدمين
users = db.get_all_users()
```

#### إدارة القاعات
```python
# إنشاء قاعة
db.create_hall(name, hall_type, capacity, color)

# جلب القاعات
halls = db.get_all_halls(active_only=True)

# تحديث العدد الحالي
db.update_hall_current(hall_id, increment)

# تبديل حالة القاعة
db.toggle_hall_status(hall_id)
```

#### الإعدادات العامة
```python
# جلب الإعدادات
settings = db.get_global_settings()

# تحديث قائمة الانتظار
db.update_outdoor_queue(increment)

# تحديث عدد المخدومين
db.update_served_count(increment)
```

#### سجل النشاطات
```python
# تسجيل نشاط
db.log_activity(user, action, details, hall_id)

# جلب السجلات
logs = db.get_activity_logs(limit=50)
```

---

## 🎨 التخصيص

### تغيير الألوان
في `main.py`، عدّل CSS:
```python
st.markdown("""
<style>
    :root {
        --primary-color: #667eea;  /* اللون الأساسي */
        --secondary-color: #764ba2; /* اللون الثانوي */
    }
</style>
""", unsafe_allow_html=True)
```

### إضافة صفحة جديدة
1. أنشئ دالة جديدة في `main.py`:
```python
def show_my_page():
    st.title("صفحتي الجديدة")
    # محتوى الصفحة
```

2. أضفها إلى القائمة:
```python
menu_items = {
    "my_page": "📄 صفحتي",
    # ...
}
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: فشل الاتصال بقاعدة البيانات

**الحل:**
1. تأكد من صحة `SUPABASE_URL` و `SUPABASE_KEY`
2. تحقق من الاتصال بالإنترنت
3. تأكد من عدم انتهاء صلاحية API Key

### مشكلة: الجداول غير موجودة

**الحل:**
1. افتح SQL Editor في Supabase
2. نفّذ ملف `setup_database.sql` كاملاً
3. تحقق من ظهور الجداول في Table Editor

### مشكلة: خطأ في تثبيت المكتبات

**الحل:**
```bash
# ترقية pip
pip install --upgrade pip

# تثبيت بيئة افتراضية
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# تثبيت المكتبات
pip install -r requirements.txt
```

---

## 🔄 التحديثات المستقبلية

- [ ] نظام الإشعارات Push
- [ ] تطبيق Mobile (React Native)
- [ ] تقارير PDF قابلة للطباعة
- [ ] لوحة تحكم للشاشات الكبيرة
- [ ] دعم متعدد اللغات
- [ ] نظام النسخ الاحتياطي التلقائي

---

## 📞 الدعم والمساعدة

### الوثائق
- [Streamlit Docs](https://docs.streamlit.io)
- [Supabase Docs](https://supabase.com/docs)
- [Plotly Docs](https://plotly.com/python/)

### المشاكل الشائعة
راجع قسم [استكشاف الأخطاء](#-استكشاف-الأخطاء)

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## 👨‍💻 المطور

تم التطوير بواسطة فريق B36 System
- **التقنيات**: Python, Streamlit, Supabase, Plotly
- **النسخة**: 1.0.0
- **التاريخ**: يناير 2025

---

## 🌟 شكر خاص

شكراً لاستخدامك نظام B36! نسعد بملاحظاتك واقتراحاتك.

**مع تحيات فريق التطوير** 🚀

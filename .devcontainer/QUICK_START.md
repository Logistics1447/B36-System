# 🚀 دليل البدء السريع - B36 System

## خطوات التشغيل في 5 دقائق

### 1️⃣ إنشاء قاعدة البيانات (دقيقتان)

1. اذهب إلى: https://supabase.com
2. سجّل دخول أو أنشئ حساب
3. اضغط **New Project**
4. املأ البيانات:
   - **Name**: B36-System
   - **Database Password**: اختر كلمة مرور قوية
   - **Region**: اختر الأقرب لك
5. اضغط **Create new project**

⏱️ انتظر دقيقة حتى يتم إنشاء المشروع...

### 2️⃣ الحصول على بيانات الاتصال (30 ثانية)

1. في لوحة تحكم المشروع، اذهب إلى **Settings** → **API**
2. انسخ:
   - **Project URL**: `https://xxxxxxxx.supabase.co`
   - **anon/public key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

### 3️⃣ إنشاء الجداول (دقيقة واحدة)

1. في Supabase، اذهب إلى **SQL Editor**
2. افتح ملف `setup_database.sql` من المشروع
3. انسخ المحتوى **كاملاً**
4. الصقه في SQL Editor
5. اضغط **RUN** أو اضغط `Ctrl+Enter`

✅ ستظهر رسالة: Success. No rows returned

### 4️⃣ إعداد المشروع (دقيقة واحدة)

```bash
# 1. افتح Terminal/CMD في مجلد المشروع

# 2. ثبّت المكتبات
pip install -r requirements.txt

# 3. افتح ملف .env وعدّل:
SUPABASE_URL=<ضع الـ URL الذي نسخته>
SUPABASE_KEY=<ضع الـ Key الذي نسخته>

# احفظ الملف
```

### 5️⃣ التشغيل (10 ثواني)

```bash
streamlit run main.py
```

🎉 **تهانينا!** سيفتح المتصفح تلقائياً

---

## 🔐 بيانات الدخول

```
اسم المستخدم: admin
كلمة المرور: 1234
```

---

## ✅ اختبار سريع

1. سجّل دخول بالبيانات أعلاه
2. اذهب إلى **إدارة القاعات**
3. اضغط زر **➕** لإضافة زائر
4. شاهد الإحصائيات تتحدث تلقائياً! 🎊

---

## 🆘 مشاكل شائعة

### ❌ خطأ: "Failed to connect to database"

**الحل:**
- تأكد من صحة SUPABASE_URL و SUPABASE_KEY في ملف .env
- تحقق من الاتصال بالإنترنت

### ❌ خطأ: "Table does not exist"

**الحل:**
- نفّذ ملف setup_database.sql مرة أخرى في Supabase SQL Editor
- تأكد من نسخ الملف **كاملاً**

### ❌ خطأ: "Module not found"

**الحل:**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 📱 التواصل

إذا واجهت أي مشكلة، تواصل معنا! 💬

---

**وقت التشغيل الكلي: 5 دقائق** ⏱️

**صعوبة الإعداد: سهل** ⭐⭐☆☆☆
